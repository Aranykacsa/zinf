#ifndef ZINF_CONFIG_H
#define ZINF_CONFIG_H

#include <stdint.h>

/* --- Globális, mindenhol ugyanaz lesz --- */
#define SECTOR_SIZE   512U
#define CRC_SIZE      4U
#define HEADER_SIZE   1U
#define PAYLOAD_SIZE  (SECTOR_SIZE - CRC_SIZE - HEADER_SIZE)
#define RAID_MIRRORS  3U

/* RAID_OFFSET: ennyivel tolódik el a második, harmadik mirror.
 * Ezt vagy fixen beállítod (pl. 30), vagy a loopdevice mérete alapján
 * számolod ki a main-ben / readerben:
 *
 *   RAID_OFFSET = total_sectors / RAID_MIRRORS;
 */
extern uint32_t RAID_OFFSET;

#endif /* ZINF_CONFIG_H */

